namespace TeraTaleNet
{
    public class LapisLazuliOre : Sundry
    {
        public sealed override int price { get { return 60; } }
        public sealed override string effectExplanation { get { return ""; } }
        public sealed override string explanation { get { return "청금석. 매우 희귀해보인다."; } }

        public LapisLazuliOre()
        { }
    }
}